# Nvim — o básico que dá produtividade

Regra: **3 por semana**. Só adiciona os próximos quando os atuais saírem sem pensar.
Nada de truque (`gg=VG`, macro, regex maluco) — isso é cavalo de pau.

## Já sei

- `<leader>ff` — achar arquivo
- `<leader>sg` — grep no projeto
- `<leader>sw` — grep na palavra sob o cursor

## Semana 1

- [ ] `<C-o>` — volta pro lugar de onde saltou
- [ ] `<C-i>` — avança de novo
- [ ] `gd` — vai pra definição sob o cursor (`<C-o>` volta)
- [ ] `ciw` — troca a palavra inteira

## Fila (não olhar ainda)

`ci"` `ci(` · `<S-h>` `<S-l>` trocar de buffer · `<C-q>` mandar busca pro quickfix,
`]q` `[q` navegar · `gr` referências · `<leader>fr` arquivos recentes ·
`%` pular pro par · `*` procurar a palavra sob o cursor
